namespace Common.Commands
{
    /// <summary>
    /// Possible Commands that we want to apply to Stepper Motors.
    /// </summary>
    public enum StepperMotorCommand
    {
        ToClockwiseLimit,
        ToCounterClockwiseLimit
    }
}